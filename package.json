{
  "name": "ts-grm-groupby-orderby-repro",
  "version": "1.0.0",
  "description": "复现 ts-grm 在 groupBy 后直接 orderBy 生成非法 SQL（order by 在 group by 之前，PG 报 syntax error at or near \"group\"）",
  "main": "repro.test.mjs",
  "scripts": {
    "test": "node --test repro.test.mjs"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "module",
  "private": true,
  "dependencies": {
    "@ts-grm/core": "0.0.12",
    "@ts-grm/sql": "0.0.12"
  }
}